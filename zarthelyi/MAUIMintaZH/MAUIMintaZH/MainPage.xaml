<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="MAUIMintaZH.MainPage"
             xmlns:local="clr-namespace:MAUIMintaZH" x:DataType="local:MainPageViewModel">
    <ContentPage.Resources>
        <Style TargetType="Button">
            <Setter Property="BackgroundColor" Value="MidnightBlue"/>
            <Setter Property="FontAttributes" Value="Bold"/>
            <Setter Property="Margin" Value="5"/> 
        </Style>
    </ContentPage.Resources>
    <Grid RowDefinitions="*,Auto" >
        <CollectionView ItemsSource="{Binding ShoppingList}" SelectedItem="{Binding SelectedItem}"
                        SelectionMode="Single" >
            <CollectionView.ItemTemplate>
                <DataTemplate x:DataType="local:ShoppingListItem">
                    <Border Stroke="Black" StrokeThickness="1" StrokeShape="RoundRectangle 20" Margin="5">
                        <Grid ColumnDefinitions="*,*,3*">
                            <CheckBox IsChecked="{Binding IsDone}" HorizontalOptions="Center" VerticalOptions="Center"/>
                            <Image Source="{Binding ImageURL}" HeightRequest="100" Grid.Column="1"/>
                            <VerticalStackLayout Grid.Column="2" VerticalOptions="Center">
                                <Label Text="{Binding Name}" FontAttributes="Bold"/>
                                <Label Text="{Binding Description}"/>
                                <Label Text="{Binding Quantity,StringFormat='Quantity: {0}'}"/>
                            </VerticalStackLayout>
                        </Grid>
                    </Border>
                </DataTemplate>
            </CollectionView.ItemTemplate>
        </CollectionView>
        <HorizontalStackLayout Grid.Row="1" Spacing="10">
            <Button Text="Add New Item" Command="{Binding AddNewCommand}" />
            <Button Text="Delete Selected" Command="{Binding DeleteSelectedCommand}"/>
            <Button Text="Share Selected" Command="{Binding ShareSelectedCommand}"/>
        </HorizontalStackLayout>
    </Grid>

</ContentPage>
